import styled from 'styled-components';

import {
  space,
  color,
  typography,
  layout,
  flexbox,
  border,
  position,
  shadow,
} from 'styled-system';

const Box = styled.div`
  ${space}
  ${color}
  ${typography}
  ${layout}
  ${flexbox}
  ${border}
  ${position}
  ${shadow}
`;

export default Box;
